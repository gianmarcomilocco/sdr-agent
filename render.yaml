services:
  # ── DEMO instance (public, no login, 2 free generations) ──────────────
  - type: web
    name: sdr-agent-demo
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    envVars:
      - key: DEMO_MODE
        value: "true"
      - key: DEMO_MAX_USES
        value: "2"
      - key: CONTACT_NAME
        value: "Gianmarco"
      - key: CONTACT_EMAIL
        value: "gianmarco.milocco@gmail.com"
      - key: CONTACT_PHONE
        value: ""
      - key: DATA_RETENTION_DAYS
        value: "365"
      - key: IP_HASH_SALT
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: APOLLO_API_KEY
        sync: false

  # ── CLIENT instance (private, login required, unlimited) ───────────────
  # Duplicate this block for each paying client and change the name.
  # - type: web
  #   name: sdr-agent-client-acme
  #   runtime: python
  #   buildCommand: pip install -r requirements.txt
  #   startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
  #   envVars:
  #     - key: DEMO_MODE
  #       value: "false"
  #     - key: DATA_RETENTION_DAYS
  #       value: "365"
  #     - key: ADMIN_USERNAME
  #       sync: false
  #     - key: ADMIN_PASSWORD_HASH
  #       sync: false
  #     - key: AUTH_COOKIE_KEY
  #       sync: false
  #     - key: ANTHROPIC_API_KEY
  #       sync: false
  #     - key: APOLLO_API_KEY
  #       sync: false
